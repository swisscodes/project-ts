import { RefObject } from "react";
import { select } from 'd3'

function selectionChart(svgRef:RefObject<HTMLElement>, data:[], dimension:object) {
  let currentSvg = svgRef.current
}

export default selectionChart